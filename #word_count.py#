import pdb

class Solution(object):
    def __init__(self):
        f = open("~/bible.txt")

    def solution(self):
        d = {}
        lines = f.readlines();
        for line in lines:
            word = line.split(" ")
            for word in words:
                if word in d:
                    d[word] = d[word]+1
                else:
                    d[word] = 1
        return d
if __name__ == '__main__':
    s = Solution()
    s.solution()
